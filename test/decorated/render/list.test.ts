// test: RenderList

describe('RenderList', () => {
  describe('many', () => {
    it('no items: does not render', () => {

    })
    it('one item: does not render', () => {

    })

    it('many items: renders list of items', () => {

    })
  })

  describe('one', () => {
    it('no items: does not render', () => {

    })

    it('one item: renders item', () => {

    })

    it('many items: does not render', () => {

    })
  })

  describe('empty', () => {
    it('no items: renders default message', () => {

    })

    it('one items: does not render', () => {

    })

    it('many items: does not render', () => {

    })
  })

  describe('render', () => {
    it('no items: render default message', () => {

    })

    it('one item: renders item', () => {

    })

    it('many items: renders list of items', () => {

    })
  })
})
